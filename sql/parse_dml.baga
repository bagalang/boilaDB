// parse_dml.baga — DML сборен вход (P3): WHERE помощници + UPDATE/DELETE.
// Тялото е в parse_dml_where.baga и parse_dml_stmt.baga — файловата
// дисциплина (§9, 400 реда) не позволява всичко в един файл.
// INSERT е в parse_insert.baga.

import "parse_dml_where.baga"
import "parse_dml_stmt.baga"
